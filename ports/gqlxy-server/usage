gqlxy-server provides CMake targets:

    find_package(gqlxy-server CONFIG REQUIRED)
    target_link_libraries(main PRIVATE gqlxy_server::gqlxy_server)

    # With standalone-server feature:
    target_link_libraries(main PRIVATE gqlxy_server::standalone)
